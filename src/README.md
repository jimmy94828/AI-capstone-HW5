# Source

This folder is reserved for optional code or configuration used to run reasoning and SPARQL queries.

The current recommended workflow is documented in `README.md` and `report.md`: run OWL reasoning with Protege, export `ontology/inferred-results.ttl`, then query the inferred graph with Apache Jena `arq` or another SPARQL engine.
